<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>Админка — Echoria Awards</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">
  <div class="container py-5">
    <h1>📊 Статистика голосований</h1>
    <table class="table table-dark">
      <thead>
        <tr><th>Номинация</th><th>Выбор</th><th>Голосов</th></tr>
      </thead>
      <tbody>
        <% rows.forEach(r => { %>
          <tr><td><%= r.nomination %></td><td><%= r.choice %></td><td><%= r.count %></td></tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>